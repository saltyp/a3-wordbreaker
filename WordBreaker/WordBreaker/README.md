Assignment 3 from CS193p

A version of Code Breaker with codes (both the master code and all guesses) that are words in the English language (instead of the codes being some colored circles or some emoji). This means that there are 26 peg choices (all 26 letters in the English language). The user is directly shown which pegs are exact matches and which are inexact matches

eg say word is "tie" and my guess is "ate", then the attempt will be shown as grey background (wrong), blue background (warmer), green background (correct placement and letter).
